<Window x:Class="TMtextng.Konfig.TextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TMtextng.Konfig"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="TextInputWindow" Height="600" Width="800"
    SizeChanged="FontSizeChange">
    <Grid x:Name="TextInputMenuGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="2"
              Grid.Column="0">
            <Grid x:Name="myCCCplace1"
                  Grid.Column="0"
                  Grid.ColumnSpan="2"/>
        </Grid>
        <Grid Grid.Row="2"
              Grid.Column="1">
            <Grid x:Name="myCCCplace2" 
                  Grid.Column="0"
                  Grid.ColumnSpan="2" />
        </Grid>
    </Grid>
</Window> 
